# 2026 广西专业录取分：100 校试点 C 批

核查日期：2026-09-12。范围为 `targets.json` 中 30 所此前未独立核查专业分的本科院校。

新增 5 校 141 条实际专业录取最低分：海南大学 65、中国石油大学（北京）21、湖南师范大学 37、华中师范大学 17、安徽医科大学 1。华中师范为公费师范与优师计划；中国石油大学保留国家专项与中外合作，海南保留中外合作与软件工程 NIIT 类别。学校或专业组汇总、强基校测或综合成绩、旧年分数未替代当前专业分。

30 校均完成本文所列入口的有边界实查：22 校当前入口只有旧年菜单或资料，2 校当前年成功空表，安徽大学真实分数入口访问失败。详见 `school-audit-notes.json` 与 `observations.json`，不能据此推断学校未招生或全部渠道尚未公开。

关键核对：

- 海南和中石油北京遍历 2026 广西真实菜单的全部 5 / 3 个组合，仅解析 `sszygradeList` 具体专业数组。
- 湖南师范物理 23、历史 14 个专业菜单与实际结果逐一吻合；实际列表超过 20 条，无截断。
- 华中师范原始位次为区间，保留文字，未转成单点；其当前返回仅提前批，普通本科及专项仍有缺口。
- 安徽医科原 PDF 第 2 页广西仅 1 行，按平行志愿“最低分”列采集；旁边“首轮投档线”独立保留，不用作专业实际分依据。成绩页和章程第 3 页已经渲染人工核对。
- 5 校当前章程已读取，用于核对普通高考总分及政策加分口径。广西 3 位组码、广西填报专业代号、完整再选科目逻辑、未明示轮次保留未知。海南“化生組”等字段按原文保留；中石油北京全国专业代码不当作广西填报代号。

公开包不含原网页、完整 API 响应、PDF、图片、个人信息及会话。原资料仅存本地 `raw/`，公开元数据保留访问日期、查询条件、原响应哈希和脱敏缓存哈希。`browser-dom-observation` 明确是实际浏览器核查摘要，其哈希指摘要文件，不冒称下载响应哈希。

离线重建：`python3 build.py`。复核：先在项目根目录运行 `node scripts/import-major-score-batch.mjs <本批目录>` 并将结果存为 `importer-preflight.json`，再运行 `python3 qa.py`。本批已完成只读导入预检：141 条新增、0 条更新；未执行 `--apply`，未修改线上数据或 git。

`extract_facts.py` 仅在本地原始缓存存在时重新提取事实；`collector.py` 和 `api_collector.py` 用于公开请求及正常匿名会话。`*-jobs.json`、`step*.json`、`entries.json` 为实际使用的公开查询清单。验证码、登录限制或资源访问失败不绕过。

后续重采约束：两个采集器共用按请求入口主机划分的进程内锁，同一主机请求串行，前次请求结束后至少间隔 1 秒；不同主机仍可并发。匿名会话初始化、正常 CSRF 获取及数据请求同样受此约束。此限制是本批结束后对公开脚本的修复，本批历史请求并未保证同主机串行；本次未重新抓取，也未更改来源日期或冻结数据。

时间估算：逐校 `elapsedEvidenceSpanSeconds` 是交错执行的首末请求证据跨度，包含其他学校工作，不能相加当作人工工时。初版采集器未记录单次请求结束时间，只有一次请求的 3 校置空；浏览器观察另以核查日期标记。全量工时应以各来源族的批次总耗时估算，不能据这些跨度作精确学校工作量承诺。

发布只使用 `PUBLIC-FILES.txt` 白名单，禁止递归复制整个目录。
