# 专业分补采 root 包

复查华中科技大学、哈尔滨工业大学、中山大学，并重新核对重庆三峡医药旧冲突；新增浙江经贸职业技术学院12条专业分。

原始下载与运行会话保留在本地 raw/，不公开。fetch.py 仅获取列明的官方页面；public_api.py 使用页面正常公开 Cookie/CSRF 流程，内存会话不写入台账，响应移除 jessionid 后另记归档摘要。2025查询只作为旧年正向对照，绝不导入2026。

浙江经贸表是图片，zjiet-reviewed-transcription.json 是双人独立逐格核对后的事实转录。6条历史、6条物理各自合计录取10人，最高/最低与原表合计一致。批次、组码、轮次原图缺失，保持待核；录取人数不转成计划人数。正文日期2026-08-13，不由URL路径推定。

复现：运行 python3 fetch.py、python3 public_api.py，再运行 python3 build.py。新抓取若内容变化，旧断言失败是需要复核的信号；须重新核对图片及返回年份再调整，不应直接修改断言使之通过。

本次哈工大显式2026参数返回2025页面；华科页面年份仅2025/2024/2023；中大2026两科普通录取均成功空响应、2025正向对照有专业数据。三峡医药两条原文矛盾仍在，原数据不改。
