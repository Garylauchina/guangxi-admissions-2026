# 批次 B 独立审阅

审阅日期：2026-09-10。结论：**最终 30 条、97 人的待导入计划转换核验通过；没有发现需要阻止这批未分组计划导入的实质错误。** 这不表示目标专业组已经补齐。

## 交大本部与医学院 18 条核验

| 项目 | 独立结果 |
|---|---|
| 年份 | 官方文章 API 标题明确为“上海交通大学2026年招生计划（含医学院）”，发布时间为 2026-07-03 09:05；原图报头为 2026-06-15，本部与医学院表题均为 2026。API 中相邻文章的 2025 标题没有用于转换。 |
| 广西列 | 目视查看同一原图，分别定位本部普通表、医学院普通表的广西表头；两表列宽不同，未复用列坐标。与既有人工转录的广西定位说明一致。 |
| 逐行转换 | 将全部 18 条输出与人工转录按行比对，schoolCode、major、plannedCount、category、sourceTable、sourceRow 全部一致；所有 ID 唯一。 |
| 校本部 | 10248 上海交通大学，15 条、35 人；原表广西总计 36 中另有文史 1 人，不计入这批物理类记录。最大项工科试验班类（信息类）为 18 人。 |
| 医学院 | 19248 上海交通大学医学院，3 条、15 人：临床医学（八）2、临床医学（口八）1、儿科学（5+3）12。未合并到本部代码；原表缩写没有擅自扩写成未经核实的培养定义。 |
| 招生类型 | 只转换两张普通计划表，原图另列的高校专项、国家专项表没有混入。没有总计行、艺术行或文史行混入。 |
| 中外合作 | 电子信息类（浦江国际学院，依托张江国际理工学院）1 人保留 category=中外合作办学，并引用学院官方说明。该页明确本科阶段英文中外合作办学项目属性；未发现页面列有被转换丢失的考试语言资格或成绩门槛。英文教学属性不应被扩写为未核实的报考资格。 |
| 组码与批次 | 18 条 group=null、batch=本科（批次待核）；没有根据校内表格行号、投档分数或同名专业猜组。全部 planStage=initial，不含征集余额。 |
| 选科与费用 | sourceTrack=理工、track=物理；再选科目仍为 unknown，学制、学费、专业代码均留空。这些是明确缺口，不是已核实为无要求或免费。 |
| 来源与 QA | 全部计划 sourceIds 均可解析；有本地原文件且有摘要的来源，SHA-256 均复算一致；原图与人工转录所锁定 hash 一致。行数、人数、学校数、未匹配数与 QA.json 一致。 |

逐行比对独立执行，未调用会改写 QA.json 的 validate.py，也未改动待导入 JSON、脚本或网站。

## 南京大学新增 12 条核验

最终文件新增南京大学 10284 的 12 条、47 人，全批为 **30 条、97 人**。已将新增 12 条逐条与 `raw/nju-gx-2026-normal.json` 对照，验证专业名称、计划人数、源专业代码、首选和再选科目、学制、类别及未知字段。全部一致。

- 每条原始数据 `nf=2026`、`ssmc=广西`、`klmc=物理类`、`zslx=普通批次`，没有跨省或跨年行。47 人与独立 `zsjhTotal` 汇总一致，汇总行没有导入。
- 12 条原文均为“物理,化学(2门科目考生均须选考方可报考)”，输出保留物理首选、化学必选和 `subjectRule=all`；学制直接保留“四年”，学费原字段为空，未猜填。
- 原始 `withZyz=false`，`zyzdm/zyz/pcmc` 为空。输出 `group=null`、广西批次待核；源类别“普通批次”另存 `sourceCategory`，没有强认定为广西省内正式批次。
- 原 API `zydm` 是学科/校内专业代码，不是已证实的广西省编填报代码。输出仅存 `sourceDisciplineCode`，`majorCode=null`，没有把如 00600、080901 当成省编代号；同代码的不同培养方向保留不同记录。
- 公开页面直接把 API `zsjhList` 渲染为专业表，未发现该请求使用分页参数；原 API 总计与 12 行合计相等。因此未发现此次“广西、2026、物理类、普通批次”查询漏页迹象，这不等于已收齐南京所有科类或所有类型。
- `planVersion` 明示当前查询版。API 没有给出首发版本日期；`planStage=initial` 只表明这批普通计划不是征集余额，不能用来证明与省级初始计划册完全一致或给南京 105 组自动配专业。
- 最终 QA 的 30 条、97 人、3 校、29 来源以及 0 个目标组补齐，均与本次独立复算一致；有本地文件及摘要的 29 条来源引用全部通过 hash 核验。

## 公开文件检查

审阅时不存在 `PUBLIC-FILES.json`，因此**没有声称已核验正式发布白名单**。对本目录顶层 `.json`、`.py`、`.md` 的文本检查及对公开请求清单和三份采集/构建脚本的定向阅读，未发现本机用户绝对路径、账号、固定认证头或会话令牌取值。

南京 `fetch_nju_public.py` 遵循官网 `tplt.js` 已公开的普通页面会话与 CSRF 流程；Cookie 和 CSRF 值只在运行时内存使用，没有固定会话值、账号或登录操作。两份南京归档 JSON 顶层已移除 `jessionid`；来源元数据区分脱敏归档 `sha256` 和原响应 `responseSha256`，复算归档摘要一致。脚本中 `Csrf-Token`、CookieJar 等字段/变量名是公开协议实现，不是会话值泄露。

`build.py` 使用相对路径读取上一级公开目标表 `../targets.json`；若发布复现文件，需一并提供该表或在说明中清楚保留这项依赖。原始网页和图片未被本次审阅加入发布清单。若随后新增 `PUBLIC-FILES.json`，应以实际清单再核对一次，避免把 raw 目录或未审阅文件整体发布。

## 范围与限制

本次独立查看了现有原图、文章 API、浦江国际学院正文及转换脚本；没有重新请求线上来源，也没有重新人工转录整张全国计划表。原图的本部首、中、尾及医学院三条与转录一致；全部 18 条转换映射和人数复算已经逐行核验。

该批能提供学校层面的 2026 广西物理类计划，不能据此把交大本部 109/110、医学院 101 或南京 105 的目标组标成已匹配。目录与目标结果目前保留待核结论，未冒称全校、全部科类或全组覆盖。来源表中的 year=2026 对入口和脚本应理解为本轮目标年份，不能把复旦“最新可读2024”的入口记录当成 2026 计划证据；目前该记录不被这 18 条计划引用。

## 最终审阅快照

- `plans-upsert.json`：`6f738c043210001d0db73d28e896c5e4f36fd08225480d0aecb249a298174aa9`
- `transcribed-sjtu-2026.json`：`cfd43fefbd01512bfaee23afba9e256648c18090d95420b5f74be0b66dbfa218`
- `sources.json`：`4d14b72e1b6fe89053ef7c9ca78b8e539848f115f80888a88414fd4e752fa857`
- `QA.json`：`99e1a2738efa13d4c38ebf3b4625cc4b25106d6623e9ad88ea3f7f12d178468e`
- `public-fetch-manifest.json`：`78e1dfab14d49ad680ada4b9d7234d6af05e20b58c93f0910ff1976ae59278ee`
- `raw/nju-gx-2026-normal.json`：`98741477b64b482446ca60589457696b9e25c92a2de8dcb9858a815af7ab3099`
